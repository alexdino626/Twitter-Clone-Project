import React, { useEffect, useContext } from "react";
const Tweet = () => {
    return (
        <>
        {/* <img src={avatarSrc} /> */}
        {/* {tweetById.media.length > 0 && <img src={tweetById.media[0]?.url} alt="img"/>} */}
        </>
    )
}